# ra-cli (npm)

This npm package installs the `ra` CLI by downloading a matching prebuilt binary from the GitHub Releases for this repository.

## Install

```sh
npm i -g ra-cli
```

## Notes

- Supported platforms: macOS and Linux (x64/arm64).
- The installed command is `ra`.
